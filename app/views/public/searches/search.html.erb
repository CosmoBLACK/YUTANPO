<h2>検索結果</h2>

<table class="table table-hover table-inverse">
  <tbody>
    <% @onsens.each do |onsen| %>
      <tr>
        <td><%= onsen.name %></td>
        <td><%= onsen.address %></td>
      </tr>
    <% end %>
  </tbody>
</table>