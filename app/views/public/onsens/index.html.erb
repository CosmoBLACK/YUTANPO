<div class="container">
  <h2 class="text-center text-white shadow-sm mt-3">温泉一覧</h2>

  <!-- タグリスト -->
  <%= render 'public/onsens/tag_list', tag_list: @tag_list %>

  <!-- 温泉カードリスト -->
  <%= render 'public/onsens/index', onsens: @onsens %>

</div>
<div class="d-flex justify-content-center">
  <%= paginate @onsens %>
</div>